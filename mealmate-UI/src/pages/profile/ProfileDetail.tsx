import React from 'react';

const ProfileDetail: React.FC = () => {
  return (
    <div>
      {/* TODO: Trang chỉnh sửa thông tin cá nhân:
        - Họ tên (editable)
        - SDT (editable)
        - Giới tính (editable)
        - Gmail (read-only, chỉ thêm được nếu chưa có gmail trước đây)
        - Mật khẩu (hiển thị dạng dấu chấm, có thể đổi)
        - Nút "Lưu"
        - Nút "Đăng xuất"
        - Tất cả trang fullscreen nếu đến từ trang trước đó thì phải có nút return to previous page
      */}
      <h1>ProfileDetail</h1>
    </div>
  );
};

export default ProfileDetail;
