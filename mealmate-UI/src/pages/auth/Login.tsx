import React from 'react';

const Login: React.FC = () => {
  return (
    <div>
      {/* TODO: Màn hình đăng nhập:
        - Form đăng nhập: Email/SDT + Mật khẩu
        - Nút "Đăng nhập"
        - Dòng "Quên mật khẩu" (link)
        - Dòng "Đăng ký tài khoản mới" (link → /register)
        - Tất cả trang fullscreen nếu đến từ trang trước đó thì phải có nút return to previous page
      */}
      <h1>Login</h1>
    </div>
  );
};

export default Login;
